Put this folder, "packed", in the TombEngine/Documentation directory.
Then take ldocpacked.bat and put it in the TombEngine/Documentation directory, too.
Finally, run:
ldocpacked.bat .
from the command line.
